'use strict'

module.exports = (babel, opts) => {
  return {
    plugins: [
      {
        visitor: {
          ArrayExpression (path) {
            const node = path.node
            node.elements.push(babel.types.stringLiteral(opts.label))
          }
        }
      }
    ]
  }
}
